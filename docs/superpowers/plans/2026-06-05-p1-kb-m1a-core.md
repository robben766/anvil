# KB-M1a:朴素 RAG 全链路(core)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 `packages/kb` 朴素 RAG 全链路库(markdown 分块 → 本地 embedding → pgvector 检索 → 带引用回链的生成)+ 检索 eval(recall@k/precision@k)+ golden set + CLI,门禁 recall@5 ≥ 0.8。

**Architecture:** 库优先(纯库,无服务壳——API/Web 在 KB-M1b);存储 pgvector(同 anvil-postgres,kb 独立 Alembic 链);生成走 `anvil_gateway.chat`(自动记账+trace);eval 指标进 `anvil_eval`(底座复用,不依赖 anvil_kb)。详见 spec `docs/superpowers/specs/2026-06-05-p1-knowledge-base-design.md`。

**Tech Stack:** Python 3.12 / SQLAlchemy 2.0 async + asyncpg + Alembic / pgvector(`pgvector` PyPI 包)/ fastembed(BAAI/bge-small-zh-v1.5,512 维)/ pytest(真 PG,anvil_test 库隔离)。

**约定(全任务适用):**
- TDD:每个任务先写失败测试再实现;测试命令 `uv run pytest packages/kb -q`(或具体文件)。
- 真 PG 测试沿用 gateway 模式:读 `ANVIL_TEST_DATABASE_URL`(本地 `postgresql+asyncpg://anvil:anvil@localhost:5434/anvil_test`),每个用例前清表。
- commit 频繁,消息用 `feat(kb): ...` / `test(kb): ...`;身份已由 repo config 钉死。
- 不要修改 `packages/core/*` 除明确列出的 eval 扩展(Task 8)。

---

### Task 1: infra 切 pgvector 镜像(本地 + CI)

**Files:**
- Modify: `infra/docker-compose.yml`(anvil-postgres 服务的 image 行)
- Modify: `.github/workflows/ci.yml`(services.postgres.image)

- [ ] **Step 1: 改两处镜像** `postgres:16-alpine` → `pgvector/pgvector:pg16`(compose 里只改 `anvil-postgres`,**不要动 langfuse 用的 postgres 服务**;ci.yml 的 postgres service 改同名镜像)。
- [ ] **Step 2: 本地重建容器并验证扩展可用**

```bash
docker compose -f infra/docker-compose.yml up -d anvil-postgres
docker exec $(docker ps -qf name=anvil-postgres) psql -U anvil -d anvil -c "CREATE EXTENSION IF NOT EXISTS vector; SELECT extversion FROM pg_extension WHERE extname='vector';"
```

Expected: 输出 vector 版本号(0.7+)。若 alpine→debian 切换出现 collation 告警,执行 `ALTER DATABASE anvil REFRESH COLLATION VERSION;`(anvil_test 同理);若容器起不来再考虑重建卷(开发数据可丢弃,但先尝试保留)。
- [ ] **Step 3: 确认 gateway 既有测试不受影响** Run: `uv run pytest packages/core/gateway -m "not live" -q` Expected: 全绿。
- [ ] **Step 4: Commit** `git commit -m "infra: switch anvil-postgres to pgvector/pgvector:pg16 (local + CI)"`

### Task 2: packages/kb 骨架 + 数据模型 + Alembic 链

**Files:**
- Create: `packages/kb/pyproject.toml`、`packages/kb/src/anvil_kb/__init__.py`、`packages/kb/src/anvil_kb/db.py`
- Create: `packages/kb/alembic.ini`、`packages/kb/alembic/env.py`、`packages/kb/alembic/script.py.mako`、`packages/kb/alembic/versions/0001_kb_tables.py`
- Create: `packages/kb/tests/conftest.py`、`packages/kb/tests/test_db.py`
- Modify: 根 `pyproject.toml`(workspace members 加 `"packages/kb"`;known-first-party 加 `"anvil_kb"`)
- Modify: `.github/workflows/ci.yml`(alembic 步骤后加一行 kb 迁移)

- [ ] **Step 1: pyproject**(hatchling src 布局,镜像 gateway 的写法)

```toml
[project]
name = "anvil-kb"
version = "0.1.0"
description = "anvil knowledge base: hand-rolled RAG pipeline"
requires-python = ">=3.12"
dependencies = [
    "anvil-gateway",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "alembic>=1.13",
    "pgvector>=0.3",
    "fastembed>=0.3",
]

[project.scripts]
anvil-kb = "anvil_kb.cli:main"

[tool.uv.sources]
anvil-gateway = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/anvil_kb"]
```

根 pyproject:members 改为 `["packages/core/*", "packages/kb"]`,isort known-first-party 加 `anvil_kb`。`uv sync --all-packages` 通过。
- [ ] **Step 2: 写失败测试 test_db.py**(真 PG:建表后能插入带 512 维向量的 chunk 并读回)

```python
import uuid

import pytest
from sqlalchemy import select

from anvil_kb.db import ChunkRow, DocumentRow

async def test_roundtrip_document_and_chunk(kb_session):
    doc = DocumentRow(id=uuid.uuid4(), title="t", source_name="s", content="全文")
    kb_session.add(doc)
    await kb_session.flush()
    chunk = ChunkRow(
        id=uuid.uuid4(), document_id=doc.id, seq=0, content="片段",
        header_path="t", start_offset=0, end_offset=2, embedding=[0.1] * 512,
    )
    kb_session.add(chunk)
    await kb_session.commit()
    row = (await kb_session.execute(select(ChunkRow))).scalar_one()
    assert row.document_id == doc.id
    assert len(list(row.embedding)) == 512

async def test_delete_document_cascades(kb_session):
    doc = DocumentRow(id=uuid.uuid4(), title="t", source_name="s", content="x")
    kb_session.add(doc)
    await kb_session.flush()
    kb_session.add(ChunkRow(id=uuid.uuid4(), document_id=doc.id, seq=0, content="c",
                            header_path="", start_offset=0, end_offset=1, embedding=[0.0] * 512))
    await kb_session.commit()
    await kb_session.delete(doc)
    await kb_session.commit()
    assert (await kb_session.execute(select(ChunkRow))).first() is None
```

conftest.py:镜像 gateway tests 的 conftest 模式——session 级 fixture 对 `ANVIL_TEST_DATABASE_URL` 跑 `alembic upgrade head`(用 `alembic.config.Config("packages/kb/alembic.ini")` 编程式调用,设置 `sqlalchemy.url`),function 级 `kb_session` 每用例 TRUNCATE kb_chunks/kb_documents。**先读 `packages/core/gateway/tests/conftest.py` 再写,保持同构。**
- [ ] **Step 3: 实现 db.py**

```python
"""kb 数据模型:kb_documents / kb_chunks(pgvector)。"""

from __future__ import annotations

import uuid
from datetime import datetime

from pgvector.sqlalchemy import Vector
from sqlalchemy import DateTime, ForeignKey, Integer, Text, func
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column

EMBEDDING_DIM = 512  # BAAI/bge-small-zh-v1.5


class Base(DeclarativeBase):
    pass


class DocumentRow(Base):
    __tablename__ = "kb_documents"

    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True)
    title: Mapped[str] = mapped_column(Text, nullable=False)
    source_name: Mapped[str] = mapped_column(Text, nullable=False, unique=True)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )


class ChunkRow(Base):
    __tablename__ = "kb_chunks"

    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True)
    document_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True),
        ForeignKey("kb_documents.id", ondelete="CASCADE"),
        nullable=False,
        index=True,
    )
    seq: Mapped[int] = mapped_column(Integer, nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    header_path: Mapped[str] = mapped_column(Text, nullable=False, default="")
    start_offset: Mapped[int] = mapped_column(Integer, nullable=False)
    end_offset: Mapped[int] = mapped_column(Integer, nullable=False)
    embedding: Mapped[list[float]] = mapped_column(Vector(EMBEDDING_DIM), nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )
```

ORM 级 cascade 提示:`DocumentRow` 上不建 relationship 也可,但 `await kb_session.delete(doc)` 走 ORM 需要 DB 级 `ondelete="CASCADE"` + 迁移里的 FK 才生效(测试里用 commit 后查表验证)。
- [ ] **Step 4: Alembic 链**。alembic.ini 镜像 gateway 的(`script_location = alembic`、`prepend_sys_path = src`);env.py 镜像 `packages/core/gateway/alembic/env.py`(async 模式),**唯一差异**:`context.configure(..., version_table="kb_alembic_version")`(online/offline 两处都要),且 url 取 `ANVIL_DATABASE_URL`(允许被编程式 override,与 gateway 同构)。迁移 0001:

```python
"""kb tables: documents + chunks with pgvector

Revision ID: 0001_kb_tables
"""
import sqlalchemy as sa
from alembic import op
from pgvector.sqlalchemy import Vector

revision = "0001_kb_tables"
down_revision = None


def upgrade() -> None:
    op.execute("CREATE EXTENSION IF NOT EXISTS vector")
    op.create_table(
        "kb_documents",
        sa.Column("id", sa.dialects.postgresql.UUID(as_uuid=True), primary_key=True),
        sa.Column("title", sa.Text, nullable=False),
        sa.Column("source_name", sa.Text, nullable=False, unique=True),
        sa.Column("content", sa.Text, nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True),
                  server_default=sa.func.now(), nullable=False),
    )
    op.create_table(
        "kb_chunks",
        sa.Column("id", sa.dialects.postgresql.UUID(as_uuid=True), primary_key=True),
        sa.Column("document_id", sa.dialects.postgresql.UUID(as_uuid=True),
                  sa.ForeignKey("kb_documents.id", ondelete="CASCADE"), nullable=False),
        sa.Column("seq", sa.Integer, nullable=False),
        sa.Column("content", sa.Text, nullable=False),
        sa.Column("header_path", sa.Text, nullable=False, server_default=""),
        sa.Column("start_offset", sa.Integer, nullable=False),
        sa.Column("end_offset", sa.Integer, nullable=False),
        sa.Column("embedding", Vector(512), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True),
                  server_default=sa.func.now(), nullable=False),
    )
    op.create_index("ix_kb_chunks_document_id", "kb_chunks", ["document_id"])
    op.execute(
        "CREATE INDEX ix_kb_chunks_embedding ON kb_chunks "
        "USING hnsw (embedding vector_cosine_ops)"
    )


def downgrade() -> None:
    op.drop_table("kb_chunks")
    op.drop_table("kb_documents")
```

- [ ] **Step 5: 跑测试到绿** Run: `uv run pytest packages/kb -q` Expected: 2 passed。
- [ ] **Step 6: CI 加 kb 迁移步骤**(gateway alembic 行后):`- run: cd packages/kb && uv run alembic upgrade head`
- [ ] **Step 7: ruff + 全量测试** Run: `uv run ruff check . && uv run pytest -m "not live" -q` Expected: 全绿。
- [ ] **Step 8: Commit** `feat(kb): package skeleton, pgvector models, alembic chain`

### Task 3: chunker(markdown 标题感知 + 字符窗口 + 表格原子,offset 精确)

**Files:**
- Create: `packages/kb/src/anvil_kb/ingest/__init__.py`、`packages/kb/src/anvil_kb/ingest/chunker.py`
- Test: `packages/kb/tests/test_chunker.py`

接口与行为(spec §5):

```python
@dataclass
class ChunkDraft:
    content: str        # == 原文 text[start_offset:end_offset],不加工
    header_path: str    # "一级标题 > 二级标题",无标题时 ""
    start_offset: int
    end_offset: int
    seq: int

def chunk_markdown(text: str, *, size: int = 600, overlap: int = 100) -> list[ChunkDraft]:
```

算法:逐行扫描并维护绝对 offset。`#`/`##`/`###` 行更新标题栈并开启新 section(标题行本身不进 chunk 内容);连续以 `|` 开头的行块为表格段(原子,无论多长单独成 chunk);section 内其余文本段按窗口切——窗口步长 `size - overlap`,窗口边界对齐到行首(不在行中间切断;一行超过 size 时允许单行超长成 chunk)。全文无标题时整体视为一个 section。空 section 不产 chunk。**不变量:`text[c.start_offset:c.end_offset] == c.content` 对所有 chunk 成立。**

- [ ] **Step 1: 写失败测试**(手算锁定;用小参数便于手算)

```python
from anvil_kb.ingest.chunker import chunk_markdown

DOC = """# 条款
## 等待期
等待期为90天。
意外不受限。
## 表格
| 项目 | 值 |
|------|----|
| 等待期 | 90天 |
说明行。
"""

def test_invariant_offsets_slice_back():
    for c in chunk_markdown(DOC, size=20, overlap=5):
        assert DOC[c.start_offset:c.end_offset] == c.content

def test_header_path_and_table_atomic():
    chunks = chunk_markdown(DOC, size=20, overlap=5)
    waiting = [c for c in chunks if "90天。" in c.content]
    assert waiting and waiting[0].header_path == "条款 > 等待期"
    tables = [c for c in chunks if c.content.startswith("| 项目")]
    assert len(tables) == 1                      # 表格原子,3 行一个 chunk
    assert "| 等待期 | 90天 |" in tables[0].content

def test_window_split_with_overlap():
    text = "a" * 50 + "\n" + "b" * 50 + "\n" + "c" * 50 + "\n"
    chunks = chunk_markdown(text, size=60, overlap=10)
    assert len(chunks) >= 2                      # 153 字符,size 60 必然切多块
    assert all(len(c.content) <= 61 for c in chunks)  # 行对齐允许 +1 容差(换行)
    for c in chunks:
        assert text[c.start_offset:c.end_offset] == c.content

def test_no_headers_whole_doc_one_section():
    chunks = chunk_markdown("纯文本一行。", size=600, overlap=100)
    assert len(chunks) == 1 and chunks[0].header_path == ""

def test_seq_monotonic():
    chunks = chunk_markdown(DOC, size=20, overlap=5)
    assert [c.seq for c in chunks] == list(range(len(chunks)))
```

- [ ] **Step 2: 跑测试确认失败**(ModuleNotFoundError)
- [ ] **Step 3: 实现 chunker.py**。建议结构:`_scan_blocks(text) -> list[Block]`(Block=:种类 text/table/header、行列表、起止 offset、当时 header_path)→ `_window(block, size, overlap)`(行对齐窗口)→ 装配 seq。窗口行对齐实现:把 block 的行(含换行符)累积,超过 size 即封一个 chunk,下一窗口从"回退 overlap 字符所覆盖的最近行首"开始;封装时直接用行的绝对 offset 计算 start/end,content 取 `text[start:end]` 保证不变量。
- [ ] **Step 4: 跑测试到绿**;若手算与实现冲突,**修测试前先用纸面推演确认哪边错**(手算锁定原则)。
- [ ] **Step 5: Commit** `feat(kb): markdown-aware chunker with exact offsets (TDD)`

### Task 4: embed.py(Embedder 协议 + fastembed 实现)

**Files:**
- Create: `packages/kb/src/anvil_kb/embed.py`
- Test: `packages/kb/tests/test_embed.py`

```python
"""本地 embedding:协议 + fastembed bge-small-zh-v1.5(512 维)。

bge 中文系列约定:query 侧加指令前缀,passage 侧不加。
"""

from __future__ import annotations

from typing import Protocol

_MODEL_NAME = "BAAI/bge-small-zh-v1.5"
_QUERY_PREFIX = "为这个句子生成表示以用于检索相关文章:"


class Embedder(Protocol):
    @property
    def dim(self) -> int: ...
    def embed_texts(self, texts: list[str]) -> list[list[float]]: ...
    def embed_query(self, text: str) -> list[float]: ...


class FastEmbedEmbedder:
    def __init__(self) -> None:
        self._model = None  # 懒加载:首次调用才下载/载入模型

    @property
    def dim(self) -> int:
        return 512

    def _ensure(self):
        if self._model is None:
            from fastembed import TextEmbedding

            self._model = TextEmbedding(_MODEL_NAME)
        return self._model

    def embed_texts(self, texts: list[str]) -> list[list[float]]:
        return [v.tolist() for v in self._ensure().embed(texts)]

    def embed_query(self, text: str) -> list[float]:
        return self.embed_texts([_QUERY_PREFIX + text])[0]
```

- [ ] **Step 1: 写失败测试**(参照 `packages/core/eval` 对 fastembed 的既有测试方式;模型已在 M4 缓存,CI 有 fastembed 缓存则直跑,没有则首跑下载)

```python
import math

from anvil_kb.embed import FastEmbedEmbedder

def _cos(a, b):
    dot = sum(x * y for x, y in zip(a, b))
    na = math.sqrt(sum(x * x for x in a))
    nb = math.sqrt(sum(x * x for x in b))
    return dot / (na * nb)

def test_dim_and_shapes():
    e = FastEmbedEmbedder()
    vecs = e.embed_texts(["等待期为90天", "保险金额"])
    assert len(vecs) == 2 and all(len(v) == e.dim == 512 for v in vecs)

def test_semantic_sanity():
    e = FastEmbedEmbedder()
    q = e.embed_query("等待期是多少天?")
    related = e.embed_texts(["本合同等待期为90天。"])[0]
    unrelated = e.embed_texts(["今天天气晴朗适合郊游。"])[0]
    assert _cos(q, related) > _cos(q, unrelated)
```

- [ ] **Step 2: 失败 → 实现 → 绿**(实现如上,可直接采用)
- [ ] **Step 3: 确认 CI 的 fastembed 缓存**:查看 ci.yml 是否已有 `~/.cache/fastembed` 的 actions/cache(M4 引入过则复用;没有则补一个 `actions/cache@v4`,key 用模型名)。
- [ ] **Step 4: Commit** `feat(kb): Embedder protocol + fastembed bge-small-zh (512d)`

### Task 5: store(类型 + 协议 + PgVectorStore,真 PG)

**Files:**
- Create: `packages/kb/src/anvil_kb/store/__init__.py`、`store/base.py`、`store/pg.py`
- Test: `packages/kb/tests/test_pg_store.py`

base.py(spec §4;数据类与协议):

```python
from __future__ import annotations

import uuid
from dataclasses import dataclass
from typing import Protocol


@dataclass
class Document:
    id: uuid.UUID
    title: str
    source_name: str
    content: str


@dataclass
class Chunk:
    id: uuid.UUID
    document_id: uuid.UUID
    seq: int
    content: str
    header_path: str
    start_offset: int
    end_offset: int
    embedding: list[float] | None = None


@dataclass
class ScoredChunk:
    chunk: Chunk
    score: float  # 统一语义:越大越相关


class VectorStore(Protocol):
    async def upsert_chunks(self, doc: Document, chunks: list[Chunk]) -> None: ...
    async def search(self, query_vector: list[float], k: int) -> list[ScoredChunk]: ...
    async def delete_document(self, document_id: uuid.UUID) -> None: ...


class SparseIndex(Protocol):  # KB-M2 实现;Day-1 只定义接口
    async def index_chunks(self, chunks: list[Chunk]) -> None: ...
    async def search(self, query: str, k: int) -> list[ScoredChunk]: ...
```

pg.py:`PgVectorStore(session_factory)`(async_sessionmaker 注入,与 gateway PostgresLedger 同构;引擎层在 `db.py` 提供 `make_engine()/make_session_factory()`,NullPool,镜像 ledger.py;**asyncpg 需注册 pgvector 类型**:

```python
from pgvector.asyncpg import register_vector
from sqlalchemy import event

def make_engine(url: str | None = None) -> AsyncEngine:
    engine = create_async_engine(url or _database_url(), poolclass=NullPool)

    @event.listens_for(engine.sync_engine, "connect")
    def _register(dbapi_connection, connection_record):
        dbapi_connection.run_async(register_vector)

    return engine
```

注意:该 listener 假设 vector 扩展已建(迁移先行);Alembic 自己的引擎不要挂此 listener。)

语义:`upsert_chunks` 以 `doc.source_name` 为幂等键——已存在同名文档则先删旧 doc(级联删 chunks)再插入新 doc + chunks,单事务;`search` 用 `ChunkRow.embedding.cosine_distance(qv)` 升序取 k,`score = 1 - distance`;`delete_document` 按 id 删。

- [ ] **Step 1: 写失败测试**(真 PG,复用 Task 2 的 conftest;向量用手造的可分辨方向,如 `[1,0,0,...]` 与 `[0,1,0,...]`)

```python
import uuid

from anvil_kb.store.base import Chunk, Document
from anvil_kb.store.pg import PgVectorStore

def _vec(i: int) -> list[float]:
    v = [0.0] * 512
    v[i] = 1.0
    return v

def _doc(name="d1"):
    return Document(id=uuid.uuid4(), title=name, source_name=name, content="全文")

def _chunk(doc, seq, vec):
    return Chunk(id=uuid.uuid4(), document_id=doc.id, seq=seq, content=f"c{seq}",
                 header_path="", start_offset=0, end_offset=2, embedding=vec)

async def test_search_orders_by_cosine_similarity(kb_store):
    doc = _doc()
    await kb_store.upsert_chunks(doc, [_chunk(doc, 0, _vec(0)), _chunk(doc, 1, _vec(1))])
    hits = await kb_store.search(_vec(0), k=2)
    assert [h.chunk.seq for h in hits] == [0, 1]
    assert hits[0].score > 0.99 and hits[1].score < 0.01  # 同向≈1,正交≈0

async def test_upsert_same_source_name_replaces(kb_store):
    d1 = _doc("same")
    await kb_store.upsert_chunks(d1, [_chunk(d1, 0, _vec(0))])
    d2 = _doc("same")
    await kb_store.upsert_chunks(d2, [_chunk(d2, 0, _vec(1)), _chunk(d2, 1, _vec(2))])
    hits = await kb_store.search(_vec(1), k=10)
    assert all(h.chunk.document_id == d2.id for h in hits) and len(hits) == 2

async def test_delete_document_removes_chunks(kb_store):
    doc = _doc()
    await kb_store.upsert_chunks(doc, [_chunk(doc, 0, _vec(0))])
    await kb_store.delete_document(doc.id)
    assert await kb_store.search(_vec(0), k=5) == []
```

conftest 补 `kb_store` fixture(基于测试引擎的 session_factory 构造 PgVectorStore)。
- [ ] **Step 2: 失败 → 实现 pg.py → 绿** Run: `uv run pytest packages/kb -q`
- [ ] **Step 3: Commit** `feat(kb): VectorStore/SparseIndex protocols + PgVectorStore (real PG)`

### Task 6: ingest pipeline(解析→分块→嵌入→入库)

**Files:**
- Create: `packages/kb/src/anvil_kb/ingest/pipeline.py`
- Test: `packages/kb/tests/test_pipeline.py`

```python
async def ingest_markdown(
    *, title: str, source_name: str, text: str,
    embedder: Embedder, store: VectorStore,
    size: int = 600, overlap: int = 100,
) -> tuple[Document, int]:
    """返回 (Document, chunk 数)。无 chunk(空文档)抛 ValueError。"""
```

流程:`chunk_markdown` → drafts 为空抛 `ValueError("document produced no chunks")` → `embedder.embed_texts([d.content for d in drafts])`(**一次批量**,不要逐条)→ 构造 Document/Chunk(uuid4)→ `store.upsert_chunks`。

- [ ] **Step 1: 写失败测试**:用假 embedder(返回 `[[seq 序号 one-hot]]`)+ 真 PG store;断言:chunk 数 == drafts 数、库里 chunk 的 content/offset 与 chunker 输出一致、空文本抛 ValueError、重复 ingest 同 source_name 不累积(复用 upsert 幂等)。
- [ ] **Step 2: 失败 → 实现 → 绿**
- [ ] **Step 3: Commit** `feat(kb): ingest pipeline (chunk -> embed -> store, idempotent)`

### Task 7: retrieve + generate(引用回链协议)

**Files:**
- Create: `packages/kb/src/anvil_kb/retrieve/__init__.py`、`retrieve/retriever.py`
- Create: `packages/kb/src/anvil_kb/generate.py`
- Test: `packages/kb/tests/test_retriever.py`、`packages/kb/tests/test_generate.py`

retriever.py(MVP 纯 dense,但入口即编排点,KB-M2 在此融合):

```python
class Retriever:
    def __init__(self, embedder: Embedder, vector_store: VectorStore) -> None: ...
    async def retrieve(self, question: str, k: int = 5) -> list[ScoredChunk]:
        return await self._vector_store.search(self._embedder.embed_query(question), k)
```

generate.py(spec §6;`chat` 可注入便于 mock,默认 `anvil_gateway.chat`):

```python
@dataclass
class Citation:
    n: int                      # prompt 中的编号(1-based)
    chunk_id: uuid.UUID
    document_id: uuid.UUID
    quote: str
    start_offset: int
    end_offset: int

@dataclass
class KbAnswer:
    text: str
    citations: list[Citation]   # 仅实际被 [n] 引用的,按首次出现序

SYSTEM_TEMPLATE = (
    "你是知识库问答助手。仅根据下列编号资料回答;引用资料处必须标注 [编号];"
    "若资料不足以回答,明确回答:资料中未找到相关内容。不得编造。\n\n{contexts}"
)

def build_messages(question: str, retrieved: list[ScoredChunk]) -> list[dict]: ...
def parse_citations(text: str, retrieved: list[ScoredChunk]) -> list[Citation]: ...
async def answer(question, retrieved, *, chat=None, session_id="anvil-kb") -> KbAnswer: ...
async def answer_stream(question, retrieved, *, chat=None, session_id="anvil-kb"):
    """async 生成器:先 yield ("sources", retrieved),逐 token yield ("delta", str),
    最后 yield ("done", KbAnswer)。"""
```

`build_messages`:contexts 形如 `[1](条款 > 等待期) 本合同等待期为90天…`(带 header_path 锚定);user 消息为 question。`parse_citations`:`re.findall(r"\[(\d+)\]", text)`,过滤 1..len(retrieved),按首次出现去重。

- [ ] **Step 1: 写失败测试**。retriever:假 embedder + 假 store(记录收到的向量,断言 query 前缀路径被走到——embed_query 与 embed_texts 返回可区分向量)。generate(全 mock,不打真 API):

```python
async def test_answer_maps_citations_back_to_chunks():
    retrieved = [_scored("等待期为90天。", seq=0), _scored("宽限期60天。", seq=1)]
    async def fake_chat(model, messages, **kw):
        return SimpleNamespace(content="等待期是90天 [1],与宽限期无关 [1]。")
    result = await answer("等待期?", retrieved, chat=fake_chat)
    assert [c.n for c in result.citations] == [1]      # 去重保序
    assert result.citations[0].quote == "等待期为90天。"

async def test_citation_out_of_range_ignored():
    ...  # 回答含 [9] 而 retrieved 只有 2 条 → citations 不含 9

async def test_answer_stream_event_order():
    ...  # 事件序列 == ["sources", "delta"×N, "done"];done 携带的 citations 与全文解析一致
```

(`answer_stream` 的 fake chat 返回 async 迭代器,镜像 gateway 流式接口——**先读 `packages/core/gateway/src/anvil_gateway/client.py` 的 stream 返回形态再写 mock**。)
- [ ] **Step 2: 失败 → 实现 → 绿**
- [ ] **Step 3: Commit** `feat(kb): retriever + citation-grounded generation (stream + non-stream)`

### Task 8: anvil_eval 扩展(GoldenCase 字段 + 检索指标,手算锁定)

**Files:**
- Modify: `packages/core/eval/src/anvil_eval/dataset.py`(GoldenCase 加 `evidences: list[str] = field(default_factory=list)`、`answerable: bool = True`;loader 透传,缺省兼容旧数据)
- Create: `packages/core/eval/src/anvil_eval/metrics/retrieval.py`
- Test: `packages/core/eval/tests/test_retrieval_metrics.py`、dataset 测试补 2 个用例

retrieval.py(纯函数,不依赖 anvil_kb):

```python
"""检索指标:recall@k / precision@k。判定 = 空白归一化后的子串包含。"""

def _norm(s: str) -> str:
    return "".join(s.split())

def _hits(chunk_text: str, evidences: list[str]) -> bool:
    t = _norm(chunk_text)
    return any(_norm(e) in t for e in evidences)

def recall_at_k(retrieved_texts: list[str], evidences: list[str], k: int) -> float | None:
    if not evidences:
        return None  # 拒答用例无 evidence,召回无定义
    top = [_norm(t) for t in retrieved_texts[:k]]
    covered = sum(1 for e in evidences if any(_norm(e) in t for t in top))
    return covered / len(evidences)

def precision_at_k(retrieved_texts: list[str], evidences: list[str], k: int) -> float | None:
    if not evidences:
        return None
    top = retrieved_texts[:k]
    if not top:
        return 0.0
    return sum(1 for t in top if _hits(t, evidences)) / len(top)
```

- [ ] **Step 1: 写失败测试(手算)**:3 evidences、top-3 覆盖其中 2 → recall=2/3≈0.6667;3 个 chunk 中 2 个命中 → precision=2/3;k 截断生效(第 4 位命中不算);全空 evidences → 两者 None;空白差异(`"等待期 为90天"` vs `"等待期为90天"`)归一化后命中;dataset:带 evidences/answerable 的行能加载,旧行缺省 `[] / True`。
- [ ] **Step 2: 失败 → 实现 → 绿** Run: `uv run pytest packages/core/eval -q -m "not live"`
- [ ] **Step 3: Commit** `feat(eval): retrieval metrics recall@k/precision@k + GoldenCase evidences`

### Task 9: golden 语料 + golden set(星辉人寿宇宙)

**Files:**
- Create: `packages/kb/golden/corpus/01-安康保障计划条款.md`、`02-理赔指南.md`、`03-产品说明.md`
- Create: `packages/kb/golden/kb.jsonl`
- Test: `packages/kb/tests/test_golden_dataset.py`(轻量:加载成功、evidence 均能在对应语料原文中找到——防 golden 自身腐烂)

**事实清单(语料与 QA 的唯一事实来源,所有数字必须一致;沿用 demo.jsonl 既有事实:等待期 90 天、重疾 100% 保额 25 种、宽限期 60 天届满效力终止):**

| # | 事实 | 落在 |
|---|---|---|
| F1 | 投保年龄 18-60 周岁,保障至 70 周岁 | 条款 |
| F2 | 等待期 90 天,意外伤害不受等待期限制 | 条款 |
| F3 | 重大疾病保险金 = 保险金额的 100%,共 25 种 | 条款 |
| F4 | 轻症疾病保险金 = 保险金额的 30%,累计最多给付 3 次 | 条款 |
| F5 | 身故保险金 = 已交保费与现金价值二者较大者 | 条款 |
| F6 | 宽限期 60 天;届满未缴,合同效力终止 | 条款 |
| F7 | 犹豫期 15 天,期内退保全额退还保费 | 条款 |
| F8 | 免责:酒驾、2 年内自杀等 6 项 | 条款 |
| F9 | 出险后应在 10 日内报案 | 理赔指南 |
| F10 | 重疾理赔:确诊材料齐全后 30 日内作出核定 | 理赔指南 |
| F11 | 理赔材料:理赔申请书/诊断证明/病理报告/身份证明 4 类 | 理赔指南 |
| F12 | 保费示例:30 周岁男性、50 万保额、20 年缴,年缴 6800 元 | 产品说明 |
| F13 | 增值服务:就医绿色通道 + 年度健康体检 | 产品说明 |

语料写作要求:每篇 40-80 行 markdown,**用 `##` 章节组织**(分块器按标题切),每个事实独立成句(短句,便于做 evidence);表格至少出现一次(条款里的"轻症给付比例表"),验证表格原子分块。语气仿真实条款("第 N 条(xxx):……")。**全部虚构,不得出现任何真实保险公司/产品名。**

kb.jsonl(12 例;evidences 为语料原文短子串 <40 字,逐条从写好的语料中复制,**不得手敲**):

| id | question | evidences 指向 | answerable |
|---|---|---|---|
| kb-01 | 等待期是多少天? | F2 | true |
| kb-02 | 等待期内发生意外伤害赔吗? | F2 后半句 | true |
| kb-03 | 重大疾病保险金给付比例是多少? | F3 | true |
| kb-04 | 轻症最多能赔几次?每次赔多少? | F4(2 条 evidence) | true |
| kb-05 | 身故保险金怎么计算? | F5 | true |
| kb-06 | 保费没按时交,过了宽限期会怎样? | F6 | true |
| kb-07 | 犹豫期多少天?期内退保退多少? | F7(2 条 evidence) | true |
| kb-08 | 酒后驾驶出险能赔吗? | F8 | true |
| kb-09 | 出险后多久内要报案? | F9 | true |
| kb-10 | 30 岁男性买 50 万保额,20 年缴,每年保费多少? | F12 | true |
| kb-11 | 这个产品覆盖牙科正畸吗? | —(语料未涉及) | false |
| kb-12 | 支持海外就医直付吗? | —(语料未涉及) | false |

每行同时给 `reference`(标准答案,供后续生成侧 RAGAS live 评测复用)。

- [ ] **Step 1: 写语料三篇**(按事实清单;写完通读一遍核对数字互不矛盾)
- [ ] **Step 2: 写 kb.jsonl**(evidence 从语料复制粘贴)
- [ ] **Step 3: 写并跑 test_golden_dataset.py**:`load_dataset("packages/kb/golden/kb.jsonl")` 成功且 12 例;对每条 evidence,断言它(空白归一化后)是对应语料**某一篇原文**的子串;answerable=false 的例 evidences 为空。Expected: 绿。
- [ ] **Step 4: Commit** `feat(kb): golden corpus (fictional insurance docs) + 12-case golden set`

### Task 10: CLI + 首跑真实报告(收官)

**Files:**
- Create: `packages/kb/src/anvil_kb/cli.py`
- Test: `packages/kb/tests/test_cli.py`(参数解析与 wiring 测试,mock 重活)
- Modify: `CLAUDE.md`(anvil 根,加 kb 一节:包定位、测试命令、golden 路径)、`README.md`(组件表加 kb 行,状态 KB-M1a)

CLI(argparse 子命令,镜像 `packages/core/eval/src/anvil_eval/cli.py` 风格):

```
anvil-kb ingest <file.md ...>            # title=文件名;source_name=相对路径
anvil-kb query "<question>" [--k 5]      # 检索+生成,打印答案与引用(走真网关,需 key)
anvil-kb eval --dataset packages/kb/golden/kb.jsonl \
              --corpus packages/kb/golden/corpus \
              [--k 5] [--recall-threshold 0.8]
```

`eval` 语义:先把 corpus 全量 ingest(幂等)→ 对每个 answerable 用例跑 Retriever → 算 recall@k / precision@k(用 Task 8 指标)→ 打印逐例表 + 均值;`mean(recall@k) >= --recall-threshold` 退出码 0,否则 1(CI 门禁语义)。**eval 不调 LLM、不依赖 key**(纯检索侧;生成侧 RAGAS live 评测在 KB-M1b 接)。拒答用例本期只打印"跳过(无 evidence)"。

- [ ] **Step 1: TDD CLI 参数与 wiring**(mock ingest/retriever;断言阈值判定与退出码)
- [ ] **Step 2: 实现 cli.py 到绿**
- [ ] **Step 3: 真跑首份报告**(需本地 anvil-postgres 与 fastembed 模型):

```bash
uv run anvil-kb eval --dataset packages/kb/golden/kb.jsonl --corpus packages/kb/golden/corpus
```

Expected: 逐例 recall@5/precision@5 表 + 均值,exit 0(recall@5 均值 ≥ 0.8)。**把完整输出原样存入任务报告**——这是验收证据和文章素材;若 <0.8,排查方向:evidence 被分块切断(调 golden 或 chunk 参数)、bge query 前缀未生效。**不许为达标而放水阈值。**
- [ ] **Step 4: 真跑一次问答冒烟**(需 DEEPSEEK key):`uv run anvil-kb query "等待期是多少天?"` Expected: 答案含 90 天且带 [n] 引用,引用映射回条款 chunk。记录输出。
- [ ] **Step 5: 文档**:CLAUDE.md / README 按上述 Modify 要点更新。
- [ ] **Step 6: 全量回归** Run: `uv run ruff check . && uv run pytest -m "not live" -q` Expected: 全绿。
- [ ] **Step 7: Commit** `feat(kb): CLI (ingest/query/eval) + first real retrieval report`

---

## 自审记录(writing-plans Self-Review)

- **Spec 覆盖**:spec §2-§6、§9 的 M1a 范围全部有任务对应;§7/§8(API/Web)明确属 KB-M1b,不在本计划。SparseIndex 仅定义(Task 5),与 spec 一致。
- **占位扫描**:无 TBD;Task 9 语料由事实清单 F1-F13 完整约束,QA 表 12 例齐全。
- **类型一致性**:`ScoredChunk(chunk, score)` 贯穿 Task 5/7/10;`Embedder.embed_query` 在 Task 4 定义、Task 7 使用;`recall_at_k(retrieved_texts, evidences, k)` 在 Task 8 定义、Task 10 使用——签名一致。
