# ─── anvil 环境变量样例 ───────────────────────────────────────────────
# 复制为 .env 后按需填写。kb-api / anvil-kb CLI 会自动加载本目录的 .env
# (依赖 python-dotenv;也可继续用 `set -a; source .env; set +a` 手动注入)。

# ── 必需 ─────────────────────────────────────────────────────────────
# PostgreSQL 连接(网关记账 + 知识库存储)。本地 compose 默认即下值。
ANVIL_DATABASE_URL=postgresql+asyncpg://anvil:anvil@localhost:5434/anvil

# DeepSeek API key:生成答案 / Contextual Retrieval 富集时需要。
# 仅做上传/检索/eval(不调 LLM)时可不填。
DEEPSEEK_API_KEY=sk-your-key

# ── 可选:第二个模型通道(百炼 qwen,后置,未填不影响 DeepSeek 链路) ──
# DASHSCOPE_API_KEY=sk-your-key

# ── 可选:可观测(Langfuse)。不填则不上报 trace,功能不受影响 ──────────
# 需先在本地 Langfuse UI 注册项目拿到 key(见 infra/docker-compose.yml)。
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_OTLP_ENDPOINT=http://localhost:3100/api/public/otel

# ── 可选:kb-api(:8400)安全 ─────────────────────────────────────────
# 设置后所有 /v1/kb 端点要求 Authorization: Bearer <key>;不设=开放访问。
# ANVIL_KB_API_KEY=change-me
# 浏览器跨域白名单(逗号分隔),默认 * 放行全部;收紧示例如下:
# ANVIL_KB_CORS_ORIGINS=http://localhost:3000

# ── 可选:P0 OpenAI 兼容 proxy(:8000)鉴权 ──────────────────────────
# 设置后 proxy 要求 Authorization: Bearer <key>。
# ANVIL_PROXY_API_KEY=change-me

# ── 仅测试用(pytest)─────────────────────────────────────────────────
# 测试库,与 dev 库隔离,避免清空开发数据。CI 里指向同一服务的另一库名。
# ANVIL_TEST_DATABASE_URL=postgresql+asyncpg://anvil:anvil@localhost:5434/anvil_test
